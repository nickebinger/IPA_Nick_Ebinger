// ------------ TRADE BALANCE ------------

// Get data (String) from the imported .json File and display as a text
// The expression was set as Source Text of a empty text layer

eval("var data=" +footage("data.json").sourceText);
data[0].Handelsbilanz;
